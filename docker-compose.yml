services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: salaryapp
      POSTGRES_PASSWORD: salaryapp
      POSTGRES_DB: salary_management
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
